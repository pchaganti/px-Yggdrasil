## [2026-05-31T09:15:50.749Z]
Adds end-to-end coverage for the command-line surface guarantees that protect an agent driving the tool — that conflicting options are refused, that a preview mode is rejected on the targets where it makes no sense, that an unknown component, rule, flow, or subcommand produces a precise actionable error rather than a confusing one, and that read-only help-style commands work even before any architecture exists. These are the first contact points an agent hits, so a regression here misroutes or silently swallows mistakes; proving them against the real binary keeps the entry surface trustworthy.
