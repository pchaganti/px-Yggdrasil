name: CLI Command Contract
description: "Output, error handling, and exit code conventions for all CLI command handlers"
reviewer:
  type: llm
scope:
  per: file
implies:
  - command-exit-codes
  - diagnostic-logging
