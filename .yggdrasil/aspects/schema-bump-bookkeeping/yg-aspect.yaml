name: SchemaBumpBookkeeping
description: "A migration must NOT write the project version itself — the migration runner is the sole writer of yg-config.yaml's version field. A migration may return bumpVersion: false to withhold the bump when it emits recoverable warnings the user must resolve first."
reviewer:
  type: llm
when:
  node:
    type: migration
