name: Relations Name-Resolution Matrix — Rust
type: test-suite
description: "Exhaustive name-resolution identification matrix for the Rust
  relation-conformance extractor — every supported use/path reference form that
  must resolve to a cross-node edge, and every shape that must stay silent (zero
  false positives). Catalogue-driven: every it('<id>') is a runCase('<id>') over a
  reference case in reference/relations/rust/, mirroring the Kotlin/Java matrices;
  resolver-level module-tree invariants stay as direct resolveRustPath assertions."
aspects: []
relations:
  - target: cli/tests/unit/relations/reference-runner
    type: uses
  - target: cli/relations/extractors
    type: uses
mapping:
  - source/cli/tests/unit/relations/extractors/rust-name-resolution-matrix.test.ts
