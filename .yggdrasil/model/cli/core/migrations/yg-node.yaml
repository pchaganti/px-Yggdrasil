name: Migrations (current)
type: migration
description: "The migration registry — an empty MIGRATIONS list at the schema version the verdict-lock redesign targets. The migrator harness reads this list; there are no current-generation transforms to register."
aspects:
  - posix-paths-output
relations:
  - target: cli/core/migrator
    type: calls
mapping:
  - source/cli/src/migrations/index.ts
