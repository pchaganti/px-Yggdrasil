name: Relations Name-Resolution Matrix — Kotlin
type: test-suite
description: "Exhaustive name-resolution identification matrix for the Kotlin
  relation-conformance extractor — every supported import/reference form that
  must resolve to a cross-node edge, and every shape that must stay silent
  (zero false positives). Isolated in its own node because the matrix is large
  enough to exceed the reviewer prompt limit if combined with other languages."
aspects: []
relations:
  [
    { target: cli/ast/runtime, type: uses },
    { target: cli/relations/core, type: uses },
    { target: cli/relations/extractors, type: uses },
    { target: cli/tests/unit/relations/extractors-c-family, type: uses }
  ]
mapping:
  - source/cli/tests/unit/relations/extractors/kotlin-name-resolution-matrix.test.ts
