## [2026-06-28T16:35:32.121Z]
Introduced the `yg portal` command — a read-only local web portal that makes the architecture graph and its verification state legible to a human who is reading and hand-editing code, complementing the agent-oriented command line. This entry covers the command scaffold only: it declares the option surface (a self-contained static-page mode, a loopback-server port with integer validation, an open-in-browser flag, and a view-only switch that disables the single write action) and routes any failure through the standard abort path. The action intentionally refuses with a clear not-available message until the extraction pipeline and local server land in later work, so the surface never exits success over unimplemented behaviour. The command is read-only by design: the only write it will ever perform is an explicit, user-initiated re-verification — never a silent mutation of source or graph.
## [2026-06-28T23:37:03.137Z]
Wired the static-emit path: with --static the command now extracts the read-only portal data contract from the committed graph and emits one self-contained offline page, printing its path; --open best-effort launches the platform browser without blocking. The local server path remains deferred. Placing the JSON-stringify-the-whole-contract seam on a separate serializer module (not here) keeps the command thin and lets the portal serialization aspects enforce that boundary on a covered pipeline node.
## [2026-06-29T06:49:53.238Z]
Wire plain `yg portal` (no static-export flag) to start the local loopback server and keep it running until interrupted, instead of erroring as not-yet-available. The static-export path is unchanged. On start the command prints the local address and whether the write action is enabled, and it shuts the server down cleanly on interrupt so the port is released. A best-effort browser open is offered but is never triggered during automated runs.
