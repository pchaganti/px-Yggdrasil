name: Determinism
description: "Same inputs must produce identical outputs — no randomness, no implicit system state, no network calls"
reviewer:
  type: llm
scope:
  per: file
implies:
  - no-nondeterminism-direct
