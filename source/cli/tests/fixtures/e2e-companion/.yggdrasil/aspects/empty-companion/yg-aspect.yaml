name: EmptyCompanion
description: A companion-bearing LLM aspect whose hook resolves to [] — no companion file is injected, but companionHash still folds.
reviewer:
  type: llm
status: enforced
scope:
  per: file
