name: NoBuildIssueMessageInEngine
description: "Engine-layer modules (core/, io/, ast/) must not call buildIssueMessage — they return structured IssueMessage data; the CLI command layer renders it. Mechanical half of what-why-next."
reviewer:
  type: deterministic
status: enforced
