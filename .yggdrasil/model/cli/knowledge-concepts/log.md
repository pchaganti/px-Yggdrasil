## [2026-06-13T05:34:22.962Z]
Knowledge content was updated to describe the content-addressed verdict lock model: how a verdict is valid only while its recorded inputs still hash to the stored value, how deterministic checks fold the results of the observations they make beyond their own subject files into that hash, and how a source change requires fresh verification rather than carrying an earlier approval forward. The configuration topic was updated for the per-tier prompt-size limit. Keeping this reference text aligned with the engine behaviour prevents an adopting agent from acting on a stale description of when a verdict is trusted.
