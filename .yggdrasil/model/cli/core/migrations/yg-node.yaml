name: Migrations (current)
type: migration
description: "The migration registry plus the registered version migrations. MIGRATIONS lists each to-X.Y.Z migration the migrator harness runs; the 5.1.0 migration removes the obsolete schemas/ directory (schema references moved into the yg schemas command)."
aspects:
  - posix-paths-output
relations:
  - target: cli/core/migrator
    type: calls
mapping:
  - source/cli/src/migrations/index.ts
  - source/cli/src/migrations/to-5.1.0.ts
