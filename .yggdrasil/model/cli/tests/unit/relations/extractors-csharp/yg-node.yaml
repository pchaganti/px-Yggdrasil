name: Relations Extractor Unit Tests — C#
type: test-suite
description: "Per-language unit tests for the C# relation-conformance dependency
  extractor — using-directive and qualified-reference uses()/calls() edge
  emission, nested-type keying, and the partially-qualified-reference guard.
  Split out of the C-family suite because the C# coverage is large enough that
  the combined reviewer prompt exceeds the tier limit."
aspects: []
relations:
  [
    { target: cli/ast/runtime, type: uses },
    { target: cli/relations/core, type: uses },
    { target: cli/relations/extractors, type: uses },
    { target: cli/tests/unit/relations/extractors-c-family, type: uses }
  ]
mapping:
  - source/cli/tests/unit/relations/extractors/csharp.test.ts
