name: Migrations (legacy v4)
type: migration
description: "Legacy schema migrations for the v4 generation (to-4.0.0, to-4.3.0). Current-generation migrations and the registry are in the migrations (current) sibling node."
aspects:
  - posix-paths-output
relations:
  - target: cli/core/migrator
    type: calls
mapping:
  - source/cli/src/migrations/to-4.0.0.ts
  - source/cli/src/migrations/to-4.3.0.ts
