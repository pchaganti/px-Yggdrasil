## [2026-05-28T08:12:48.649Z]
Added unit tests for the new references tracking in collectTrackedFiles. The tests verify: LLM aspect references appear as graph/aspects entries; AST aspects references are excluded by the LLM guard; reference paths that coincide with the node own mapping are classified as source/source (SOURCE step priority); shared references across two aspects are deduplicated to one entry; absence of references produces no spurious entries; per-node classification is independent (same path is graph/aspects for a node that does not own it and source/source for the node that does own it).
