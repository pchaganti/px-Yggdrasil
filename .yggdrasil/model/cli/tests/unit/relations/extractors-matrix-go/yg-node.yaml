name: Relations Name-Resolution Matrix — Go
type: test-suite
description: "Exhaustive name-resolution identification matrix for the Go
  relation-conformance extractor — every supported import/reference form that
  must resolve to a cross-node edge through the import-path resolver, and every
  shape that must stay silent (zero false positives). Catalogue-driven: every
  it('<id>') is a runCase('<id>') over a reference case in reference/relations/go/,
  mirroring the Kotlin/Java matrices."
aspects: []
relations:
  - target: cli/tests/unit/relations/reference-runner
    type: uses
mapping:
  - source/cli/tests/unit/relations/extractors/go-name-resolution-matrix.test.ts
