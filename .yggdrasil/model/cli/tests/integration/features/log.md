## [2026-05-16T13:42:18.151Z]
Rename migration-4.3-to-4.4.test.ts → migration-4.2-to-4.3.test.ts. Part of flattening 4.3.0+4.4.0 into single 4.3.0 release.
## [2026-05-27T07:55:50.864Z]
Updated the richGraph() test fixture to use v5 reviewer format for the audit-logging aspect (reviewer: { type: llm }) instead of no reviewer field. Required because parseAspect now returns aspect-reviewer-missing for aspects without reviewer, which would prevent the aspect from loading into graph.aspects.
## [2026-05-27T13:17:00.265Z]
Added migration-4.3-to-5.0 and multi-tier-approve integration tests to graph coverage. Both tests verify v5 reviewer tiers behavior: migration test checks config/aspect YAML transformation from v4 to v5, multi-tier test checks LLM tier routing, AST bypass, drift-state baseline recording, and tier-change drift detection. Node mapping updated to satisfy type-strict-orphan enforcement.
## [2026-05-27T13:55:00.100Z]
Migration integration tests rewritten for the corrected migration semantics: providers preserved as named tiers, multi-provider STOP without active, secrets-file inspection, version bump gated through runVersionUpgrade. The earlier chain-migration test now uses runVersionUpgrade rather than runMigrations to match the runner-driven bump.
## [2026-05-28T05:09:58.733Z]
Integration tests rewritten for the incremental migration contract. The legacy chain test now asserts that with only the next applicable migration registered the version advances exactly one step. A dedicated framework test file exercises the runner contract directly with mock migrations: incremental persistence between steps, chain stop on bumpVersion false, no-op when current version equals the highest target, semver ordering of input migrations, behaviour with missing or malformed yg-config.yaml, and durable persistence when a later migration throws.
## [2026-05-28T05:31:08.159Z]
Three dedicated content-transformation integration tests added — one per registered migration step. Each seeds a realistic v(X-1) fixture on disk (config + node yamls + aspects + secrets where relevant) and asserts the v(X) shape after migrateToX runs. Reference shapes pulled from git tags v3.0.0 and v4.2.0. Covers single-provider and multi-provider configs, scalar and mapping reviewer fields, nested aspect directories, secrets credential narrowing, idempotency on already-current input, and stop-before-aspects semantics on unrecoverable input.
