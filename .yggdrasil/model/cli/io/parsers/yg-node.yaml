name: IO Parsers
type: module
description: Filesystem parser adapters for graph definition files. Split into two sub-nodes — graph-shape parsers (architecture, aspect, node, flow) and configuration parsers (config, schema, secrets) — so the reviewer sees fewer than 5 files per call. In-memory parsers (log-parser, file-when-parser, when-parser) are in cli/core/parsing.
