name: Relations Name-Resolution Matrix — TypeScript and PHP
type: test-suite
description: "Exhaustive name-resolution identification matrix for the TypeScript
  and PHP relation-conformance extractors — every supported import/use/reference
  form that must resolve to a cross-node edge, and every shape that must stay
  silent (zero false positives), including the per-clause function/const guard in
  grouped PHP use imports. Isolated in its own node so the combined matrix stays
  within the reviewer prompt limit."
aspects: []
relations:
  [
    { target: cli/ast/runtime, type: uses },
    { target: cli/relations/core, type: uses },
    { target: cli/relations/extractors, type: uses },
    { target: cli/tests/unit/relations/extractors-c-family, type: uses }
  ]
mapping:
  - source/cli/tests/unit/relations/extractors/typescript-name-resolution-matrix.test.ts
  - source/cli/tests/unit/relations/extractors/php-name-resolution-matrix.test.ts
