name: E2E Tests — Fill-Stage Semantics
type: test-suite
description: "Subprocess E2E pinning three fill-stage behaviours: the
  deterministic gate seeded from a cached-valid enforced deterministic refusal
  (distinct from the fresh-violation gate), the no-reviewer-configured
  fail-closed disposition leaving a pair unverified with no verdict written and
  exit 1, and an LLM consensus verdict that does not reach a satisfied majority
  being recorded as refused. Real spawned binary with the in-process mock
  reviewer."
aspects: []
relations: [ { target: cli/tests/e2e/support, type: uses } ]
mapping:
  - source/cli/tests/e2e/cli-fill-semantics.test.ts
