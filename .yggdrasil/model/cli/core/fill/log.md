## [2026-06-13T03:12:07.303Z]
Filling unverified verdicts is the single place a reviewer or a deterministic check executes, so it concentrates the fail-closed write discipline: a verdict is recorded only when the reviewer actually returned one, deterministic checks run first and gate the paid LLM dispatch, and a node's verdicts and source fingerprint advance together only when every enforced rule on it passes.
## [2026-06-13T03:16:12.633Z]
Every catch that returns a fallback instead of re-throwing emits a diagnostic line, and every path written to agent-facing output is normalized to forward slashes, so a swallowed read failure is never invisible and output never leaks platform-specific separators.
## [2026-06-13T05:33:38.794Z]
Positive closure now advances a node's source fingerprint and log baseline only when every enforced rule is freshly verified against the current code in this run and the node was not gated. A stored approval from an earlier run can no longer stand in for re-verification, so a source change can never be recorded as reviewed without an actual review. Reference files contributing to a rule are hashed from their raw bytes so any edit invalidates the cached verdict. Garbage collection retains recorded results for a node whose effective rule set cannot be computed, rather than discarding them, so an uncomputable applicability state never silently drops prior verification.
