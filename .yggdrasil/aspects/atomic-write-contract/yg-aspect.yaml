name: AtomicWriteContract
description: "Persistence-adapter files must not call writeFile or appendFile from node:fs/promises directly — all file writes must go through atomicWriteFile() from io/atomic-write to ensure crash-safe atomic updates."
reviewer: ast
