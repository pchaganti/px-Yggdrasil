name: TestDeterministic
description: "Test suites must produce identical results on repeated runs — no Math.random(), no wall-clock assertions, fresh temp dirs per test, no ambient environment dependencies."
reviewer: llm
