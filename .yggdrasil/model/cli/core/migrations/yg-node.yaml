name: Migrations (current)
type: migration
description: "Current-generation schema migrations and the migration registry — the v5 transform, its aspect-status defaults helper, and the index that lists all migrations. Legacy v4 migrations are in the migrations-legacy sibling node."
aspects:
  - posix-paths-output
relations:
  - target: cli/core/migrator
    type: calls
mapping:
  - source/cli/src/migrations/index.ts
  - source/cli/src/migrations/to-5.0.0.ts
  - source/cli/src/migrations/aspect-status-defaults.ts
