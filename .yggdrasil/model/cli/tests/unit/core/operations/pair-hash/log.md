## [2026-06-12T12:10:18.903Z]
Added three new test groups to cover previously untested frozen-contract behaviors: (1) hashReadObservation high-byte correctness — asserts that a Buffer containing bytes >= 0x80 produces the same digest as createHash('sha256').update(raw).digest('hex') computed independently, closing the gap where the old latin1 round-trip implementation would silently produce wrong hashes for binary content; (2) scope.files fold determinism — asserts computeLlmInputHash is stable across key-insertion-order variations in the predicate object, that a files-filtered scope differs from an unfiltered scope, and that the canonical JSON serialization of the predicate is pinned to a known string; (3) references sort comparator — asserts that passing a two-element references array in reverse insertion order produces the same hash as the sorted order, confirming the internal sort is functional.
## [2026-06-12T12:18:00.671Z]
Rewrote the vacuous key-insertion-order-shuffle test: the previous test used two scope objects whose keys were byte-identical (single-key predicate atom), making the test incapable of failing even if canonicalization was broken. Replaced with a two-key predicate atom ({ path, content }) where one scope has keys in insertion order path→content and the other has content→path. Added a non-vacuity guard asserting that JSON.stringify of the two raw objects differs before canonicalization, ensuring the test can actually detect a regression. Added a test confirming that the reference description (third tuple element) folds into the LLM hash, covering a dimension of the frozen contract that was previously untested.
