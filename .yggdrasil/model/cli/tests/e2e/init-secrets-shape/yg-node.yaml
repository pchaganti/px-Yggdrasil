name: E2E Tests — init API-key secrets shape
type: test-suite
description: "Subprocess E2E proving the reviewer API-key secrets overlay shape — for
  every key-requiring provider the credential sits in the bootstrapped tier's config
  block (reviewer.tiers.<tier>.config.api_key), a 1:1 overlay over yg-config.yaml and
  NOT a provider-level bucket, and the real yg check parses the merged config without
  an unknown-reviewer-key error and reaches a clean green check. Driven entirely over
  the public CLI surface: a hand-authored config + secrets overlay verified through the
  spawned binary, with no coupling to the CLI's internal init or config modules."
aspects: []
relations: []
mapping:
  - source/cli/tests/e2e/cli-init-secrets-shape.test.ts
