## [2026-05-26T08:33:12.484Z]
AspectVerificationResult.providerError boolean → required errorSource discriminator. Mirrors AspectResponse change in cli/llm/shared. Removes deprecated optional fields added as bridge in Tasks 1+2. ApproveResult.aspectViolations also updated to include errorSource so LlmApproveResult extends it without type incompatibility.
## [2026-05-29T07:15:28.478Z]
Extended DriftNodeState with optional structureTouchedFiles map and added 'structure-touched' to TrackedFileLayer. The structureTouchedFiles field records, per structure aspect, the set of files that the aspect's check.mjs touched during its last approved run. It is intentionally optional so baselines written before this feature remain compatible: a missing field means cold start. The field must survive draft-toggle operations unchanged (clearDraftAspectsFromDriftState must not remove it) so a subsequent non-draft approve can compare against the previous baseline. Also clarified the errorSource JSDoc on AspectVerdict to document that the 'astRuntime' tag covers both AST and structure runner crashes, preserving the tag name for backward compatibility with stored baselines.
