## [2026-05-31T07:46:28.879Z]
Adds end-to-end coverage proving the reviewer-configuration validator behaves as designed. The configuration file declares which reviewer tiers exist, which provider each uses, and how many independent runs must agree (consensus); a malformed configuration must be rejected loudly rather than silently degrade enforcement. This suite spawns the real binary against hand-authored broken configurations and asserts each documented rejection reason fires with a non-zero exit, so a future change that weakens configuration validation is caught immediately instead of shipping a graph that no longer enforces its own reviewer contract.
## [2026-05-31T21:54:43.746Z]
Removed tests CR1 and CR2 from cli-config-tier-validation.test.ts. CR1 asserted config-reviewer-legacy-format for the pre-tier v4 reviewer shape; CR2 asserted config-reviewer-mixed-format for a config mixing tiers and legacy keys. Both codes were removed when legacy detection was relocated to migration-only. Old-shape configs now yield config-reviewer-unknown-key from the generic reviewer-mapping validation.
