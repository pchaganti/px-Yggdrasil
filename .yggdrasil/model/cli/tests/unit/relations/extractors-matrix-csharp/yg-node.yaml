name: Relations Name-Resolution Matrix — C#
type: test-suite
description: "Exhaustive name-resolution identification matrix for the C#
  relation-conformance extractor — every supported using/qualified-reference
  form that must resolve to a cross-node edge, and every shape that must stay
  silent (zero false positives), including nested-type keying and
  partially-qualified guards. Isolated in its own node because the matrix is
  large enough to exceed the reviewer prompt limit if combined with others."
aspects: []
relations:
  [
    { target: cli/ast/runtime, type: uses },
    { target: cli/relations/core, type: uses },
    { target: cli/relations/extractors, type: uses }
  ]
mapping:
  - source/cli/tests/unit/relations/extractors/csharp-name-resolution-matrix.test.ts
