## [2026-05-31T08:36:52.312Z]
Adds end-to-end coverage for the remaining ways a named entry point carries obligations across a component boundary. A port is how a critical rule reaches a consumer that depends on a provider; the subtler mechanics — several entry points on one provider, a consumer depending on more than one, an obligation that only warns or is still being drafted, an obligation that applies only to some consumers, and re-review when the provider changes its contract — must all behave as specified, because a gap here lets a consumer escape an obligation it should inherit. The suite proves each against the real binary.
