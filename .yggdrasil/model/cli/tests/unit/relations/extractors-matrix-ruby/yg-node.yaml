name: Relations Name-Resolution Matrix — Ruby
type: test-suite
description: "Exhaustive name-resolution identification matrix for the Ruby
  relation-conformance extractor — every supported require_relative/constant
  reference form that must resolve to a cross-node edge through the dual-axis
  resolver (path link + shared SymbolTable), and every shape that must stay silent
  (zero false positives: load-path require, reopening ambiguity, C1 lexical
  shadowing, dynamic const_get/autoload, stdlib, unmapped, intra-node). Catalogue-
  driven: every it('<id>') is a runCase('<id>') over a reference case in
  reference/relations/ruby/, mirroring the Go/Kotlin/Java matrices."
aspects: []
relations:
  - target: cli/tests/unit/relations/reference-runner
    type: uses
mapping:
  - source/cli/tests/unit/relations/extractors/ruby-name-resolution-matrix.test.ts
