name: Integration Pipeline Tests
type: module
description: "Parent node for integration pipeline tests — children group by concern: approve flows, aspect-references behaviour, aspect-status lifecycle, and the core build/check/validation pipelines."
aspects: []
relations: []
