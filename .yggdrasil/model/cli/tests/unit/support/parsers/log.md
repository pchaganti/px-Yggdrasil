## [2026-05-26T09:57:44.112Z]
Add test cases for language field on AspectDef: single-language array, multi-language array, and LLM aspect without language field.
## [2026-05-27T07:40:44.345Z]
Added three tests for object-form reviewer parsing: { type: ast }, { type: llm, tier: expensive }, and array-valued reviewer (invalid shape defaults to llm). Covers new AspectReviewerSpec object branches added to aspect-parser.ts in the v5 reviewer-tiers refactor.
## [2026-05-27T07:56:05.289Z]
Rewrote aspect-parser.test.ts to use the new ParseAspectResult discriminated union API. All old tests that expected parseAspect to throw now check r.ok === false and inspect r.errors. Added new describe blocks for v5 happy paths (AST, LLM no tier, LLM with tier) and v5 error paths (legacy string, missing reviewer, null reviewer, type-missing, type-invalid, AST+tier, unknown key, multi-error, empty mapping). Updated all inline YAML to include reviewer: { type: llm } since parseAspect now requires it.
## [2026-05-28T06:44:57.638Z]
Added aspect-parser-references.test.ts to the Graph Parser Tests node mapping. This test file covers the new references field parsing in aspect-parser.ts — specifically: shorthand string normalization to { path, description: undefined }, explicit object form with description, order preservation across mixed forms, and absent field yielding undefined. The file uses afterEach cleanup with rmSync to remove mkdtempSync temp directories, matching the cleanup pattern required by the test-deterministic aspect.
## [2026-05-28T06:54:08.759Z]
Added parser-phase validation tests for the 'references' field in aspect-parser-references.test.ts. Nine new test cases cover the rules introduced in the parser: rejection of references on AST aspects, rejection of blank and whitespace-only paths, rejection of paths that escape the repository root (leading slash, Windows drive letter, tilde, and excess parent-directory traversal via depth counting), acceptance of inner double-dot segments that do not traverse above the root, rejection of duplicate normalized paths, and acceptance of an empty references array (the empty-array warning is deferred to validator phase).
## [2026-05-28T11:11:09.122Z]
Added unit tests for the new 'status' field on aspect parsing. Covers each of the three valid values (draft, advisory, enforced), the absent-field path that lets the resolver apply the default, and the rejected case that returns the 'aspect-status-invalid' error code. Uses the established disk-based fixture pattern (tmpdir + writeAspectFixture helper) so each case is hermetic and the cleanup hook removes the entire tmp tree between tests.
## [2026-05-28T11:23:30.539Z]
Extended aspect-status-parsing tests with implies object form coverage: bare-string entries leave impliesStatusInherit absent; object entries with status_inherit 'strictest' and 'own-default' populate it correctly; invalid values surface 'implies-status-inherit-invalid'. Added a re-throw test to guarantee the catch-block heuristic in the parser does not over-match — an unknown-field error whose message mentions status_inherit in the allowed-keys hint must still propagate as a bare throw, not be remapped to the structured error code.
## [2026-05-28T11:30:34.623Z]
Added four new node-parser tests exercising the status override surface on aspect attachments. The cases cover the bare-string form (no override produced), the object form carrying a status, the mixed bare-plus-object form, and a port-level aspect entry with a status. Each test writes a yg-node.yaml fixture to a temporary directory, invokes the parser, and asserts that the side table is present only when an override was supplied. Test data is self-contained and the temp directory is cleaned up in afterEach to keep runs deterministic and isolated.
