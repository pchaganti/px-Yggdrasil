export const summary = 'yg-config.yaml fields: version, reviewer model, quality thresholds, parallelism, reviewer timeout';

export const content = `# Configuration (yg-config.yaml)

\`yg-config.yaml\` lives in \`.yggdrasil/\` and controls the reviewer and
runtime behavior. It is generated by \`yg init\` and updated by \`yg init --upgrade\`.

## yg-config.yaml reference

Full annotated example:

\`\`\`yaml
version: "4.3.0"

reviewer:
  model: claude-opus-4-5        # LLM model for yg approve
  timeout_seconds: 120          # Timeout per reviewer call
  parallelism: 3                # Max concurrent approve calls

quality:
  min_coverage_pct: 80          # Minimum % of repo files with graph coverage
  max_unmapped_files: 50        # Hard cap on unmapped file count
\`\`\`

### version

Matches the Yggdrasil CLI version that generated this config. The CLI
refuses to load a config with a version it doesn't recognize. Run
\`yg init --upgrade\` to update to a newer version.

### reviewer.model

The LLM model used for \`yg approve\`. Use the most capable model available
for your team's budget — reviewer accuracy directly affects rejection quality.
Changing the model does not cause existing approvals to expire.

### reviewer.timeout_seconds

Per-call timeout in seconds. Increase for large nodes with many source files.
Default: 120 seconds.

### reviewer.parallelism

Maximum concurrent LLM calls during a batch approve (\`--aspect\` or \`--flow\`).
Higher parallelism reduces wall-clock time but increases concurrent API spend.
Default: 3.

## Provider configs

Yggdrasil reads the LLM API key from the environment, not from config.
Set the appropriate environment variable before running \`yg approve\`:

\`\`\`bash
export ANTHROPIC_API_KEY=sk-ant-...          # Anthropic Claude
\`\`\`

The exact environment variable name depends on the provider configured in
\`reviewer.model\`. The CLI will emit an error if the variable is missing
when \`yg approve\` is invoked.

Provider selection is implicit in the model name. The CLI resolves the
correct API client based on the model identifier format.

## Secrets

\`yg-config.yaml\` must not contain API keys or tokens. Commit it to the
repository — it contains no secrets. All credentials go in environment
variables.

Do not put any of the following in \`yg-config.yaml\`:
- API keys or tokens
- Passwords or passphrases
- Service account credentials
- Webhook secrets

If \`yg-config.yaml\` is accidentally committed with credentials, rotate the
credentials immediately — do not rely on git history cleanup.

## Quality thresholds

Two quality controls gate the repo health check:

### min_coverage_pct

Minimum percentage of repo source files that must be in at least one node's
mapping. \`yg check\` emits a warning (not error) when below this threshold.

Coverage warnings do not block CI — they are informational. Use the
\`max_unmapped_files\` hard cap to enforce a ceiling.

### max_unmapped_files

Hard cap on the number of unmapped files. When exceeded, \`yg check\` emits
an error that blocks CI.

Use both together: \`min_coverage_pct\` as a percentage floor and
\`max_unmapped_files\` as an absolute ceiling. Start with permissive values
during onboarding and tighten over time.

## Upgrading

\`\`\`bash
node <path-to-yg> init --upgrade
\`\`\`

Reads the existing \`yg-config.yaml\`, applies migrations, and writes the
updated version. Run from the repository root only. Review the diff before
committing — migrations may change defaults.
`;
