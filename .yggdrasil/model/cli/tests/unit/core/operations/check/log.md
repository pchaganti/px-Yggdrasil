## [2026-05-28T19:52:54.267Z]
Strip dangling design/plan section references from JSDoc and test descriptions. The design doc lives in .plans/ which is gitignored and will be deleted; references like 'spec §7', 'design §12.1', 'Task 14' become stale pointers to non-existent files. Replaced with self-contained prose. No behavior change.
## [2026-05-30T21:05:41.980Z]
Drift attribution was sharpened so a dependent node is no longer fully re-reviewed when it drifts only because a cross-node file read by one of its graph-aware deterministic checks changed. Previously such a change was un-attributable to any specific check, so the whole node — including its human-language reviewer aspects — was conservatively re-verified; that re-verification is paid and non-deterministic, so it produced flaky, costly churn on nodes whose own code never changed. Now the change is attributed to the specific deterministic check that reads the file (via the per-node record of which files each check touched), so only that local, free check re-runs and the human-reviewer verdicts carry forward unchanged. The motivation is correctness-preserving cost control: a check that only reads cross-node files cannot affect what a human-language reviewer sees, so re-running it alone is exactly right.

Specific to this node: tests lock that both synthetic per-check identity causes render a message naming the owning check rather than an unknown source.
## [2026-05-30T21:30:17.159Z]
Tests were added that exercise every branch of the cascade-cause explanation helper directly: aspect-content vs aspect-metadata causes, the two synthetic per-aspect identity causes, reference-file attribution across zero/one/many declaring aspects (including that deterministic aspects are excluded from the declared-by set since only human-language aspects carry reference files), parent-node and dependency causes with and without a resolvable path, and the generic fallback for any other tracked layer. The motivation is to lock each agent-facing explanation variant so a regression that garbled or misattributed a cause would fail here.
## [2026-05-30T21:35:22.237Z]
This test suite was narrowed to cover only the check command's drift classification and cascade-cause message rendering. The log-handling tests were moved to a sibling suite because the combined reviewer context had grown past the per-node size budget, and a suite that exceeds that budget risks the reviewer's prompt being truncated and unchanged code being wrongly rejected near the cut. Splitting by concern — drift/cascade here, log handling separately — keeps each suite focused and safely within the budget. The motivation is reviewer reliability, not a change to what any test asserts.
## [2026-05-30T21:54:05.740Z]
Internal names that still referred to the retired single-file and graph-aware reviewer kinds were renamed to reflect the one unified deterministic kind: the per-aspect record of which files a deterministic check read, the synthetic drift-identity key summarising that set, the tracking layer those cross-node files sit on, and the error tag for a crash inside a non-reviewer (deterministic) runner. This is a clarity-only rename with no behaviour change — the verification mechanics, drift detection, and carry-forward are identical; only the identifiers and the persisted token strings changed so the code no longer speaks of reviewer kinds that no longer exist. Baselines were migrated in place to the new token names.
## [2026-05-31T06:24:48.171Z]
An internal name was changed for clarity: the per-aspect record of files a deterministic check read, its synthetic drift-identity key, and the tracking layer for those files were renamed from a deterministic-prefixed form to a check-prefixed form, so the identifiers name the check.mjs artifact they describe. This is a clarity-only rename with no behaviour change — drift detection, attribution, and carry-forward are identical; only the identifiers and the persisted token strings changed, and in-repo baselines were migrated in place to the new tokens.
## [2026-05-31T07:08:06.355Z]
A test was added pinning that the check command reports a structured aspect-implies-cycle error, without throwing or crashing, when a cycle is introduced after a node already has an approved baseline — the path that previously escaped to the unclassified-error handler. The motivation is to lock the always-clean cycle diagnostic so a regression that reintroduced the crash on the baseline-present path would fail here.
## [2026-05-31T09:10:50.492Z]
Updated the suite to assert the corrected behavior after fixing product defects surfaced by this comprehensive E2E effort. The suite previously pinned the actual (defective) behavior so it would stay green either way; now that the defects are fixed, the assertions assert the intended contract instead, so the suite guards the corrected behavior going forward.
## [2026-06-01T07:03:57.687Z]
Removed the second argument (graph) from all five describeIdentityCause calls in the test file, matching the updated function signature. The function no longer accepts a graph parameter; the graph object passed previously was never used inside the function body. All assertions remain identical — only the argument count changed.
