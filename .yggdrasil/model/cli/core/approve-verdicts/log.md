## [2026-05-31T15:45:20.909Z]
This unit holds the pure construction and merge of per-aspect verdicts, kept separate from the reviewer-orchestration engine so each node review context stays within the per-node size budget. The orchestration engine had grown large enough that any further addition pushed its reviewer prompt past the budget that guards against context-window truncation, which can make a reviewer falsely reject unchanged code near the cut. Splitting the deterministic verdict logic out restores headroom and keeps each reviewer prompt focused on a cohesive concern. The logic is pure: it maps reviewer results to approved or refused verdicts and decides which prior verdicts to carry forward when an aspect could not be validly evaluated in a given run, so it carries no I/O or LLM dependencies.
## [2026-06-03T07:05:08.093Z]
Per-aspect verdicts are aggregated and persisted so each effective aspect carries one clean pass/fail outcome into the node baseline, including infrastructure-error carry-forward when a reviewer could not run. This lets a cascade re-approve of a single changed aspect reuse the prior verdicts of the node's other aspects instead of re-judging everything.
