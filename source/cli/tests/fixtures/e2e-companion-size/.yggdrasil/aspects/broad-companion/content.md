# Broad companion

The reviewed document must describe a payload contract.

## Rule: payload is described

The document must contain at least one non-empty line describing what the paired
payload provides.

### Why

So the paired payload's purpose is traceable from the document.
