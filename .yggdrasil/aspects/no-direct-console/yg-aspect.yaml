name: NoDirectConsole
description: "STUB — content added in Phase 4. Files outside bin.ts and formatters/ must not call console.log/warn/error directly; diagnostic output goes through utils/debug-log."
reviewer: ast
