## [2026-05-31T09:15:51.280Z]
Adds end-to-end coverage for upgrading an existing project and for rejecting a malformed reviewer configuration. Upgrading must install the correct instruction file for each supported agent platform and must carry old configuration shapes forward safely; a broken or unrecognized configuration must be refused with a clear reason rather than silently degrading enforcement. These paths run rarely but are high-consequence — they touch every adopter at upgrade time — so proving the installer matrix and the configuration-coercion edges against the real binary guards a moment where a silent failure would be costly.
