# Scenario document is reviewed against the whole spec suite

This aspect ships a companion.mjs that resolves EVERY spec in the uses-related
node as a companion. The reviewer therefore sees the subject scenario plus all
paired specs in the `<companions>` block (multiple companions per unit).

Verify the scenario describes a workflow that at least one of the paired specs
exercises.
