name: MigrationIdempotent
description: "Migration code must be safe to run twice without corrupting state — all write operations are idempotent and the migration checks the current version before acting."
reviewer:
  type: llm
