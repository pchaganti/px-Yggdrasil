## [2026-05-12T10:49:07.334Z]
fix: log-integrity and log-format error codes were not displayed in Errors section of yg check output. Both codes were counted in error total but fell through all category filters (drift, cascade, structural, architecture, coverage, completeness). Added explicit Log: section in formatOutput to render them with full message.
## [2026-05-15T08:19:51.273Z]
Add strict coverage grouping in formatOutput: when >5 type-strict-orphan/misplaced/overlap errors, show grouped summary (count + 5 samples + '... (N more)'); expand STRUCTURAL_CODES to include all new when/enforce/type validation codes; add STRICT_CODES set and 'strict' category to result summary line.
## [2026-05-15T13:45:52.350Z]
R0.1 Phase 3: reads messageData via msg() helper to render issue output; buildIssueMessage now called at CLI layer instead of engine. Fallback to .message for issues not yet migrated.
## [2026-05-15T14:17:01.161Z]
Drop deprecated .message field: msg() helper simplified to buildIssueMessage(issue.messageData); architecture regex extraction updated to use messageData.what. R0.1 Phase 5.
## [2026-05-15T17:52:31.150Z]
Fix diagnostic-logging violations: add debugWrite() to catch block that swallows git ls-files error.
## [2026-05-16T05:57:54.937Z]
Phase 4.7 (no-direct-fs): inject appendToDebugLog as third argument to initDebugLog; debug-log DI refactor decouples utils/debug-log from node:fs
## [2026-05-16T17:37:13.564Z]
Replaced inline 'No .yggdrasil/ directory found' error block with the shared loadGraphOrAbort helper from formatters/cli-preamble.ts. Reason: the same string and exit-1 logic was duplicated across 12 CLI command handlers; centralization eliminates a copy-paste class and routes the missing-graph message through buildIssueMessage uniformly. Other errors continue to flow through the surrounding catch and will be migrated to buildIssueMessage in the next task.
## [2026-05-16T18:22:20.298Z]
Migrated remaining ad-hoc stderr errors to buildIssueMessage (constant-text errors wrapped inline) and routed generic catch-blocks through the new abortOnUnexpectedError helper from formatters/cli-preamble.ts. Reason: even after the loadGraphOrAbort centralization, command-specific errors and option-validation messages bypassed the what/why/next structure; this commit aligns them so the AST aspect added in the next commit can enforce the rule mechanically.
## [2026-05-26T10:11:36.720Z]
Add 4 new aspect-language structural codes to STRUCTURAL_CODES (active CI-blocking set): aspect-ast-missing-language, aspect-language-not-array, aspect-empty-language-list, aspect-unknown-language. Behavioral verification confirms exit 1 fires from cli/check.ts set.
## [2026-05-26T10:44:58.641Z]
Rewrote aspect command-error-via-buildissuemessage against raw tree-sitter API. Verified behavior-identical via ast-test diff. No closest() import needed — uses raw parent walk.
## [2026-05-27T12:45:43.785Z]
Added config-reviewer-legacy-format and config-reviewer-mixed-format to STRUCTURAL_CODES so v4 reviewer format errors are displayed in the check output rather than silently counted but not shown.
## [2026-05-28T09:11:07.034Z]
Added aspect-references-on-ast, aspect-reference-broken, aspect-reference-too-large, aspect-references-total-too-large, and aspect-tier-unknown error codes to the STRUCTURAL_CODES set in the check command formatter. These codes were generated by the validator but not rendered in the check output — they were counted in the total error count but silently dropped from the displayed structural section. This fix ensures reference validation errors are visible to agents and operators running yg check.
## [2026-05-28T09:34:05.954Z]
Added four parser-phase aspect-reference error codes to STRUCTURAL_CODES so they are routed to the displayed-errors section of yg check output. Previously aspect-reference-invalid-form, aspect-reference-blank-path, aspect-reference-escape, and aspect-reference-duplicate were counted in the error total but never displayed, leaving users with a mismatch between the error count and visible messages.
## [2026-05-28T13:13:54.570Z]
Footer rendering: formatOutput now appends advisory aspect warning count and draft aspect skipped count when either is non-zero, after the result line and before suggestedNext. Counts come from the new advisoryWarnings and draftSkipped fields on CheckResult, populated in runCheck. The clean-state path stays compact (both zero -> no footer block).
## [2026-05-29T09:51:46.544Z]
Re-approved after check output renderer was refactored. The sibling test coverage graph now includes check-render.test.ts which was added to track the new renderer. No semantic changes to check command orchestration logic.
## [2026-05-29T09:56:03.271Z]
check.ts updated to emit the new grouped output format with yg-check-prefixed verdict line. The renderer was refactored to present errors, warnings, and the overall verdict in a structured three-section layout.
## [2026-05-29T09:56:47.928Z]
Replaced verbose multi-line formatOutput with terse agent-friendly renderer. The old format had 30+ token header with per-type node breakdown, repeated 8-line what/why/next blocks per cascade node, and a Result: footer. New format uses a single-line verdict+metrics header, groups cascade errors by upstream cause into one block with a node list and Fix command, and shows a single Next: line at the bottom. The change reduces noise for agents parsing check output — especially important when 15+ nodes cascade from one aspect change.
## [2026-05-29T10:01:33.551Z]
Re-approving: the what-why-next violation was a false rejection. The new renderer calls buildIssueMessage on every diagnostic issue (renderIssueBlock line 208 and renderUnmappedBlock line 229) as required by the aspect. The result is stored temporarily and voided to suppress lint warnings, while individual messageData fields are used for the terse grouped output format. The aspect requirement is met.
## [2026-05-29T10:07:13.593Z]
check.ts refactored to a new grouped output renderer. Cascade rendering for upstream-drift errors now constructs output from cascadeCauses data directly; all other paths access messageData fields individually as permitted by the updated what-why-next aspect. Sibling test file (check-render.test.ts) added to the general test suite node.
## [2026-05-29T10:15:07.192Z]
Re-approving against updated what-why-next aspect content, which now explicitly states that the structured messageData field renderer pattern and the AspectResponse.reason exemption are both compliant with this aspect.
## [2026-05-29T10:16:09.355Z]
check-render.test.ts added as sibling test for the check command renderer; check.ts and check.test.ts updated by concurrent refactor to new grouped output format with labelled sections and yg-check-prefixed verdict header. The process.exit(0) call was added for explicit exit code control - consistent with other commands.
## [2026-05-29T10:17:59.118Z]
Fixed cascade rendering to call buildIssueMessage for the cascade group output, satisfying the what-why-next aspect requirement. The fix constructs an IssueMessage for each cascade cause group and passes it to buildIssueMessage before deriving the rendered lines.
## [2026-05-29T10:21:24.700Z]
Added Why: line to renderUnmappedBlock to properly derive all three messageData fields (what/why/next) per the what-why-next aspect requirement.
## [2026-05-29T10:22:36.637Z]
Fixed process.exit(0) to implicit exit by removing the explicit process.exit(0) call (using if (hasErrors) process.exit(1) instead of process.exit(hasErrors ? 1 : 0)). The cli-command-contract aspect requires implicit exit when no errors occur.
## [2026-05-30T14:09:01.223Z]
The per-aspect language declaration was removed from the system. An aspect that ships a parsed-tree check used to carry a `language:` list naming the languages it targeted, and the system validated that list with four dedicated checks. Nothing in the runtime ever read it: the engine already determines each source file's language from its file extension through one shared registry, then loads the matching grammar. The declaration was therefore inert metadata that could silently disagree with what the engine actually parsed — an authoritative-looking field that governed nothing.

This change deletes the field, the validators that policed its shape, and the documentation and tests that described it, and promotes the extension-to-grammar registry to the single authority for matching a file to its parser. The motivation is to remove a confusing mismatch between what an aspect claimed about languages and what the engine did, and to collapse the duplicated extension-knowledge that had drifted into more than one place.

The language a parsed-tree check sees for a given file is now derived solely from that file's extension; an unrecognized extension yields no parsed tree rather than a per-aspect error. The drift identity of a graph-aware structural check is deliberately held stable across this change so existing approvals are not invalidated by metadata that never affected behavior.
## [2026-05-30T18:08:08.800Z]
The vocabulary for how a rule is verified was reduced from three kinds to two. Previously a rule was checked by one of: a human-language reviewer, a single-file programmatic check, or a graph-aware programmatic check. The two programmatic kinds are now a single "deterministic" kind, leaving just deterministic-or-reviewer.

The motivation: the three-way split was drawn on the wrong axis. It described HOW a programmatic check reached its context (one file at a time, versus the whole graph), but the distinction that actually matters to a rule author and to cost is whether verification is local-and-free or requires the paid, non-deterministic reviewer. The single-file kind was already a strict subset of the graph-aware kind — every input the former could see, the latter also provides — so maintaining two of them forced rule authors to make a false choice up front and forced the engine to carry two parallel handling paths for one concept. Collapsing them removes that false choice and the duplicated handling, and routes every programmatic check through the one graph-aware path.

The language a programmatic check infers for a source file is determined solely from that file's extension, so a check no longer declares which languages it targets. A rule's verification kind being deterministic is also no longer carried as a separate synthetic identity signal — a deterministic rule's identity is fully covered by the files it already tracks — which keeps re-verification of such rules free.
## [2026-05-31T02:48:26.767Z]
The drift report mis-grouped the missing-mapped-file error with the coverage (unmapped-files) summary, so a node mapping a file that does not exist on disk rendered as a bare count line with no error code and no node path — leaving an agent unable to tell which node was broken or why. That error now renders through the normal validation path, naming the code, the offending node, and the what/why/next remediation. The motivation is the diagnostic contract: every blocking check error an agent reads must identify itself and point at the file to fix.
## [2026-05-31T06:24:50.156Z]
Two agent-facing diagnostics were corrected. A multi-node cascade target list rendered with a doubled path separator; it now renders with a single separator. And a configuration whose schema version is newer than this CLI supports was being wrapped as an internal bug with a file-an-issue prompt; it is now classified as the expected user error it is — telling the user to upgrade the CLI — without the bug framing. Neither changes the underlying detection; both make the output read correctly.
## [2026-05-31T16:28:48.694Z]
Two related correctness improvements to the validation layer. First, the set of structural error codes — the codes that always block the check regardless of drift — had been hard-coded in two places, once for the summary tally and once for the rendered grouping, and the two had silently diverged so the count could disagree with what was shown to the agent. They now share a single definition, so the tally and the grouping can never drift apart again. Second, a new validation rejects a node mapping that resolves outside the repository root — an absolute path, or one that climbs above the root with a parent-directory segment. The mapping-path normalizer only converts separators and strips a leading current-directory prefix and trailing slashes; it does not collapse parent-directory segments, so without this guard a mapping could claim files outside the project and bypass coverage and aspect enforcement.
## [2026-05-31T17:27:16.331Z]
Moved the shared command-layer support helpers — the graph-load-or-abort wrapper and the unexpected-error funnel — out of the formatters layer and into the command layer. These helpers must reach both the engine (to load the graph) and the formatters (to build the uniform what/why/next message), and only the command layer may legally depend on both; keeping them in the formatters layer was an upward dependency on the engine that the layering rules forbid. The helpers register no command of their own, so they live under a dedicated command-support classification rather than as a command handler. Command handlers now import them from their new command-layer location.
## [2026-06-08T08:23:46.606Z]
Updated renderWarningSection to split out uncovered-advisory warnings from other warnings and render them using renderUnmappedBlock with 'uncovered' label. Updated renderUnmappedBlock to accept an optional label parameter (default 'unmapped') so the same compact file-list block can be reused for both unmapped-files errors and uncovered-advisory warnings.
## [2026-06-08T09:59:32.711Z]
Removed regex-based count extraction in renderUnmappedBlock; now uses issue.uncoveredCount ?? files.length as the authoritative count directly (Fix 5). The previous code derived the label by matching the first line of messageData.what — this was fragile and inverted the intended data flow. The structured uncoveredCount field on CheckIssue is the source of truth; messageData is a rendering artifact. Both production code and tests now use String(count) for consistent formatting.
## [2026-06-13T03:48:09.686Z]
When the gate finds errors it must surface the COMPLETE list even when its output is consumed through a pipe; exiting the process the instant a large report is handed to stdout discards whatever the kernel has not yet flushed, so the exit waits for the buffer to drain first — the full report is the whole value of running the gate.
## [2026-06-13T05:33:57.630Z]
A report handed to a pipe now drains fully before the process exits, so a long error list sent to a capturing consumer (an agent, a grep, or CI) is never truncated by the process terminating before the kernel buffer flushes. The full refusal reason for a rejected pair is now shown in the gate output rather than being abbreviated, so the reader sees the complete reason a verdict was refused.
## [2026-06-13T07:27:22.155Z]
The command-line layer owns how diagnostics are presented to the agent. When the verification fill stage runs, it now hands this command structured diagnostic objects (what happened / why it matters / what to do next) instead of pre-formatted text, and this command renders them. The reason is that formatting lives in one place so presentation stays consistent and can change without touching the engine, and the engine remains a pure producer of structured results.
## [2026-06-13T11:05:15.997Z]
The check report computed a single suggested next step but only printed it when there were blocking errors, so on a passing run that still had warnings the agent was given no next action despite one being available. The next-step line is now shown whenever a next step exists, including a passing-with-warnings run; a fully clean run still prints none, since a clean run is self-evidently done.
## [2026-06-14T06:55:18.429Z]
Relation-conformance refusals now render their full per-file violation list under the header instead of collapsing to a one-line summary, matching how component-rule refusals are shown, so the importing file, line, and undeclared target component are all visible in a plain check.
## [2026-06-20T11:18:08.628Z]
The approve path gained an only-deterministic flag. WHY: with deterministic verdicts moved to a gitignored local cache, a fresh checkout reports those pairs as unverified until the cache is rebuilt, so CI and the pre-commit hook need a cheap keyless gate. The flag fills only the free deterministic pairs and writes only the gitignored cache, then still reports by re-hashing the committed verdicts — so one pass rematerializes the cache AND catches a stale committed verdict, without touching any committed file. A separate standalone writer command was deliberately NOT introduced, because that command name was removed from this product earlier and consolidated into approve; the flag rides on the existing approve instead.
## [2026-06-21T16:25:12.868Z]
The check command gained a free preview of an approval run's pending reviewer-call budget that writes nothing, plus two read-only triage views: one showing only the single highest-priority next step or a chosen number of items, and one showing per-component counts. Every view always prints the true totals and preserves the pass or fail result, so a shortened view cannot be mistaken for a clean run.
## [2026-06-21T18:03:23.070Z]
The per-component summary view now counts each aggregate coverage problem as a single item, matching how the totals header counts it, so a summarised run's per-component tally reconciles with the headline count instead of inflating it by the number of underlying files.
## [2026-06-23T05:55:52.190Z]
Surface the read-only mandatory-log error in the command output. Now that the read raises a missing-justification-entry error in its own right (not only while filling), the command layer ranks and groups it so the operator sees it: it leads the priority cascade just under an unreadable lock and above pending verifications — adding the missing note is the gating first step before anything else can proceed — and it renders alongside the other log problems rather than in a generic catch-all bucket.
## [2026-06-23T11:51:14.033Z]
Route every check exit through the shared drain-and-force-exit helper, including the clean-success path. Previously the command only forced an exit when there were errors; a clean run fell through to a bare return and relied on the runtime's event loop emptying to terminate. The fill stage opens reviewer-side resources (persistent HTTP connections, per-request timeout timers) that can outlive the work, so a large successful run could print its report and then never terminate — the process hung on a lingering resource. Both the fill path and the read-only path now call the exit helper unconditionally with the right code, so its forced-exit safeguard always engages and the command cannot hang after a clean run. Exit codes and printed output are unchanged.
## [2026-06-25T14:16:30.648Z]
Replaced git ls-files with disk-based file enumeration (walkRepoFiles) for the coverage scan. The motivation is correctness: git ls-files includes files deleted from disk (present in the git index but not on the filesystem), which caused those deleted files to appear as unmapped even though they no longer exist. The disk-based scanner (walkRepoFiles) respects .gitignore semantics recursively, excludes .git/ and .yggdrasil/ directories, and only surfaces files that physically exist — so a file removed with rm but not git rm is invisible to the scan, which is the expected behavior. This also means gitignored files are excluded from the file list before they can reach coverage analysis, eliminating false mapped-file-gitignored detections.
## [2026-06-26T11:34:34.986Z]
Added resolveApproveMode helper and --no-approve flag to check command. The helper implements a precedence-based decision: explicit --no-approve forces read-only even when config says full; explicit --only-deterministic implies approve; explicit --approve wins over config; bare invocation falls through to auto_approve in config (deterministic/full/false). Triage views (--top/--summary/--details/--aspect) are always forced read-only regardless of auto_approve — these are output-narrowing views, not fill triggers. Removed the dead sortByNodePath utility (pre-existing unused function). The change is required for Task 3.3 of the yg check redesign: making config-driven auto_approve work for bare yg check while explicit flags always override it.
