# Audits check

The requirement's acceptance prose must be realized by the enforcer check injected
as a companion (the check named in this document's `enforced-by` front-matter).

## Rule: check realizes the requirement

The companion check must enforce what the document's acceptance prose describes.
