## [2026-05-15T16:03:14.564Z]
Delegate migration orchestration to core/migrator-runner.ts — thin wrapper now calls coreRunVersionUpgrade for the runMigrations/updateConfigVersion sequence, then handles CLI-layer concerns (refreshSchemas, architecture file creation, installRulesForPlatform)
## [2026-05-15T17:52:31.258Z]
Fix diagnostic-logging violations: add debugWrite() to multiple catch blocks that swallow errors without re-throwing.
## [2026-05-16T08:39:06.989Z]
Use buildIssueMessage for non-TTY already-exists warning: satisfies what-why-next aspect added via init flow
## [2026-05-16T18:22:20.812Z]
Migrated remaining ad-hoc stderr errors to buildIssueMessage (constant-text errors wrapped inline) and routed generic catch-blocks through the new abortOnUnexpectedError helper from formatters/cli-preamble.ts. Reason: even after the loadGraphOrAbort centralization, command-specific errors and option-validation messages bypassed the what/why/next structure; this commit aligns them so the AST aspect added in the next commit can enforce the rule mechanically.
## [2026-05-16T18:54:56.640Z]
Wrapped --upgrade requires --platform error in buildIssueMessage. All option-validation messages in init now use the structured what/why/next form.
## [2026-05-16T19:12:22.102Z]
Wrapped '.yggdrasil exists but is not a directory' error in buildIssueMessage. All raw error writes in init.ts now route through buildIssueMessage or abortOnUnexpectedError.
## [2026-05-27T12:26:25.442Z]
Updated writeReviewerConfig to emit v5 tiers format: reviewer.tiers.standard with provider, consensus, and config block instead of v4 flat provider keys. The v5 format aligns with the config parser which rejects v4-format configs at load time.
## [2026-05-27T13:54:33.050Z]
Comment cleanup — removed version-numbered phrasing so the comment describes what the code does rather than which schema iteration introduced it.
## [2026-05-28T05:09:49.559Z]
Init upgrade command no longer threads fromVersion or toVersion through to the runner. The runner reads the current project version from yg-config.yaml itself, runs every applicable migration incrementally, and reports the landed version. The wrapper simply forwards the migration list, refreshes schemas, and installs rules.
## [2026-05-29T10:05:51.315Z]
Test suite for this command was updated to reflect the redesigned yg check output format. The old format had section headers (Structural:, Cascade summary:), per-node cascade repetition, and a Result: footer. The new format uses a single-line verdict header, grouped cascade blocks, and Why:/Fix: labelled output. Tests that asserted the old format strings were updated to the equivalent new format assertions.
## [2026-05-30T10:23:11.222Z]
Two bespoke error messages printed by the setup command — the non-interactive-terminal guard and the missing-graph-version guard on the upgrade path — were brought into line with the command error-output convention used everywhere else in this command: a user-facing failure written to standard error is prefixed with 'Error: ' so operators and scripts can recognize it consistently. Only the message prefix changed; the conditions and exit behavior are unchanged.
## [2026-05-31T02:48:26.943Z]
The non-interactive upgrade path (the one agents and CI use) discarded the migration warnings returned by the upgrade and always reported success, even when a migration was withheld because a config could not be safely migrated — so the agent saw a success message and a zero exit while the version was silently left un-bumped. That path now surfaces the warnings and exits non-zero when a migration is withheld, matching the interactive path and the documented fix-and-re-run contract. The no-mutation safety was already correct; only the missing signal was added. The motivation is that a withheld migration must never be reported as success.
## [2026-05-31T02:55:22.074Z]
Two debug-log lines emitted when an optional config or secrets file is absent named the module and the operation but omitted the underlying error detail, leaving a future reader without the actual reason the read failed. They now include the error message, matching the established diagnostic-logging convention used elsewhere in this command. The motivation is that every diagnostic an agent or maintainer reads should carry module, operation, and the concrete error — even on an expected not-found branch — so the signal is uniform.
## [2026-05-31T03:04:38.908Z]
The non-interactive upgrade now distinguishes an incomplete (withheld) upgrade from a completed one that merely emitted informational notices. It exits non-zero with the fix-and-re-run guidance only when the chain stopped early and the version was left un-advanced; when the upgrade completed it surfaces any informational notices and reports success. The earlier version treated the mere presence of any notice as a withheld upgrade, which wrongly failed a successful upgrade that happened to emit an informational notice. The motivation is an accurate success/failure signal: complete upgrades succeed, incomplete ones fail loudly, and notices are never silently dropped.
## [2026-05-31T16:03:31.581Z]
Replaced the hand-inlined path-separator normalization with calls to a single shared helper. The same small idiom — convert backslash separators to forward slashes, and in most places also strip a trailing slash — had been copied across many modules, so the normalization rule lived in dozens of places at once and any change to it risked drifting them out of step. Consolidating it behind one well-named helper means the rule lives in exactly one spot and each call site reads by intent instead of by a repeated regex. Behavior is unchanged: the helper bodies are byte-for-byte equivalent to the expressions they replace, and the full test suite passes identically.
## [2026-05-31T17:27:17.334Z]
Moved the shared command-layer support helpers — the graph-load-or-abort wrapper and the unexpected-error funnel — out of the formatters layer and into the command layer. These helpers must reach both the engine (to load the graph) and the formatters (to build the uniform what/why/next message), and only the command layer may legally depend on both; keeping them in the formatters layer was an upward dependency on the engine that the layering rules forbid. The helpers register no command of their own, so they live under a dedicated command-support classification rather than as a command handler. Command handlers now import them from their new command-layer location.
## [2026-06-13T05:34:22.760Z]
Initialization and upgrade now ensure the repo-root .gitattributes carries a single line marking the verdict lock file as generated, so review tools collapse its diff and language statistics exclude it. The lock is committed to the repository but is machine-written, so treating it as generated keeps human review focused on hand-authored changes. The behaviour is idempotent: it creates the file with just that line when absent, appends the line exactly once (preserving existing content and ensuring a separating newline) when the file exists without it, and does nothing when the line is already present. It runs on both fresh initialization and every upgrade so existing adopters pick it up.
## [2026-06-13T10:37:56.058Z]
The interactive setup flow described the reviewer as running during a standalone approve step that has been removed. The reviewer now runs as part of the unified check-with-approve fill, so the onboarding prose was corrected to name the current command. Text-only fix.
## [2026-06-14T06:55:18.739Z]
Project setup, both fresh and upgrade, now ensures the repository ignore file excludes the relation-conformance cache directory. That directory is a rebuildable local artifact; without the ignore entry an adopter could commit it and then fail a coverage check over an unmapped, machine-generated file.
## [2026-06-14T22:16:41.043Z]
The local symbol-index cache that the relation-conformance analyzer writes while scanning code dependencies is relocated under the tool directory so that all rebuildable, derived state lives in one dot-prefixed, ignored location rather than at the repository root. Project setup and upgrade now ignore the new cache location instead of the old root-level one. The cache must stay untracked because committing it would make the coverage gate treat it as an unmapped source file; the ignore handling remains idempotent so repeated setup or upgrade runs add the entry at most once and preserve any existing ignore content.
## [2026-06-15T05:25:27.142Z]
Consolidated all Yggdrasil-derived local state into a single in-graph gitignore so initialization manages it in one place. Rebuildable caches (the relation pass's per-language symbol-index cache, the opt-in debug log) and secrets (provider API keys) all live under the graph directory and must never be committed: a committed cache trips the coverage gate as an unmapped file the moment it is tracked, and secrets must never reach the repo. Initialization now writes and maintains the local gitignore idempotently — created with the full set on a fresh setup, and on an upgrade only the missing lines are appended while any pre-existing entries the adopter added are preserved — so existing adopters pick up the complete set without losing their own customizations. The earlier split, where the symbol cache was instead excluded via a separate repo-root gitignore line, is retired; that split was the root cause of example and fixture graphs leaking their generated caches as untracked files, because the repo-root mechanism never propagated into those nested graph directories.
## [2026-06-15T05:40:47.203Z]
The non-TTY 'already exists' notice and the schema-copy degraded-operation notice are warnings, not errors, so they are written to stdout. The command-output contract reserves stderr exclusively for genuine errors and routes non-error (warning) output to stdout, matching the rest of this command's surface; both notices had inconsistently gone to stderr and are corrected.
## [2026-06-16T09:52:40.423Z]
Two changes. The upgrade-required check now compares a project graph version against the graph SCHEMA version the CLI supports, not the package version, so a code-only release that does not change the graph format no longer triggers a perpetual upgrade loop. The Ollama connection probe now disables the model reasoning trace and allows a longer cold-load window, matching the real reviewer, so a working large local model is not falsely reported as unreachable.
## [2026-06-19T09:19:57.543Z]
Initialization and upgrade no longer create or refresh a per-project schemas directory. A fresh project is set up without one, and upgrading an existing project relies on the version migration to remove it. The motivation: schema field references are now embedded in the tool and read through a command, so per-project copies of the schema files are redundant and would only drift between projects or go stale after an upgrade. The advertised wording for the upgrade was also corrected to stop claiming it refreshes schemas.
## [2026-06-20T11:18:08.876Z]
init now ignores the deterministic-verdict cache and marks the committed lock files as generated for review tools. WHY: deterministic verdicts moved to a gitignored local cache that must never be committed, so a fresh setup and every upgrade must add it to the ignore list. The single committed lock also became two committed files, so the generated-file attribute widened from one fixed name to a glob covering the family.
## [2026-06-20T15:38:23.409Z]
The interactive setup used to store the reviewer credential at a provider-level key inside the local, gitignored secrets overlay. That overlay is a one-to-one, same-shape merge onto the committed reviewer configuration, where the reviewer section legitimately accepts only the default-tier pointer and the set of named tiers — anything else is rejected when the configuration is parsed. As a result the credential the user had just entered made the very next verification run fail to load the configuration at all. The credential now lands inside the bootstrapped tier's own settings block, which is also where a hand-written secrets overlay is expected to put it. The reason placement is per-tier rather than per-provider: distinct tiers may target distinct providers (and even distinct keys for the same provider), so a credential is a property of a tier, not of a provider. The separate reviewer-reconfigure path carried the identical mistake and was corrected the same way.
