## [2026-05-31T09:29:48.396Z]
Adds end-to-end coverage of the language-model reviewer mechanics that only appear with larger or mixed inputs, all driven deterministically by an in-process stand-in for the model. The points under test are the system's own behavior: that source too large for one request is split and every part is sent, that a component carrying both a locally-checked rule and a model-checked rule contacts the model only for the latter, that several model-checked rules each get their own request, that a transient rate-limit response is retried, and that a recorded negative verdict is re-rendered by the status command — blocking when the rule is enforced and merely warning when advisory — without contacting the model again. These complete the proof that the most-used reviewer path behaves as designed, independent of any model's judgment.
