version: "5.1.0"

quality:
  max_direct_relations: 10

reviewer:
  default: standard
  tiers:
    standard:
      provider: ollama
      consensus: 1
      config:
        model: sonnet
        endpoint: http://localhost:11434
    deep:
      provider: ollama
      consensus: 3
      config:
        model: opus
        endpoint: http://localhost:11434
