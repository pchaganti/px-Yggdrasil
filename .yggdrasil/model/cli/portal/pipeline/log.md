## [2026-06-28T18:13:50.730Z]
The extraction pipeline derives an audit data contract by reusing the existing read-only verification functions rather than recomputing anything. It loads the graph committed-only, walks the repository files, and reads severities and coverage from the standard check run, per-pair verification states from lock verification, and the expected-pair denominator from pair computation. The governing constraint is count parity: every number the audit surface shows must equal what the standard check reports, so the pipeline never re-implements a count or re-hashes inputs — it calls the engine and reads the result. Generation time is the only impurity and is stamped after assembly, kept out of the pure contract module. The live relation boundary is left honestly unknown here rather than fabricated clean, because the relation pass that would populate it is wired in a later step; reporting it as clean before computing it would be a false assurance the honesty model forbids.
## [2026-06-28T20:18:46.339Z]
The count-derivation helper that buckets each verification pair into the verified / refused / unverified totals was made reachable from tests so the bucketing decision can be exercised directly with a synthetic pair list. The real lock is all-green, so against real data the bucketing only ever takes its verified branch — the branches that route a code refusal, a missing or stale verdict, an oversized-prompt gate trip, and a companion-resolution failure were never observed. The honesty model the audit surface rests on requires that none of those four ever read as green: a refusal is the only code "no"; the other three are all "not yet verified" and must fall into the unverified total, never be silently dropped and never inflate the verified total. Exposing the helper lets a test assert that identity (verified + refused + unverified equals the full expected-pair set) and that the two fail-closed gate trips specifically land in the unverified total. No behavior changed; this only widens the seam so the guarantee is mechanically checked rather than assumed.
## [2026-06-28T21:52:07.899Z]
Extended the read-only extraction pipeline so the emitted audit data carries per-node honest state, the aspect/flow/type catalogues, and the fan-in/out hubs, residue, and rule-grouped worklist — not just the headline counts. Every derived field reuses the engine's own read-only results (the per-pair verdict states, the effective-aspect cascade, the issue grouping, the coverage scan) rather than recomputing anything, so the audit view can never drift from what the build itself reports. Three states are kept deliberately distinct and never collapsed into a single "all clear": a node that owns code but carries no rule reads as no-rule, an aggregating rule reports that it judges nothing, and a rule that resolves to zero subjects reports that it verifies nothing — absence of red is never presented as full coverage. The derivation modules are grouped under the single pipeline component because the architecture forbids a pipeline component depending on another pipeline component; co-locating them keeps their imports internal and avoids a forbidden cross-component edge. The live dependency-boundary and the suppression inventory are intentionally left honestly unknown/empty for now: deriving them needs the dependency-parse and the suppression-scan, which live in component types this pipeline is not permitted to depend on, and opening that edge is a deliberate architecture decision left to the maintainer rather than worked around here.
## [2026-06-28T22:12:58.271Z]
Hardened the per-node derivation so reading the reviewer configuration tolerates its absence: when no reviewer is configured at all, an LLM rule's row still renders with its kind and billed cost rather than throwing, so the audit data is produced even on a graph that has not set up a reviewer yet. This keeps the read-only extraction total — it never fails to emit because of optional configuration that a consumer may legitimately not have set.
## [2026-06-29T05:25:02.173Z]
Rewired the extraction pipeline to reach the engine ONLY through the single facade, removing its direct dependencies on every engine subsystem. The pipeline now calls the facade and uses the data contract, nothing else. This also completes two audit surfaces that were previously left honestly empty: the full live dependency boundary (real code dependencies with no declared relation; declared relations with no static code backing; and dependencies whose target type the architecture forbids) and the live inventory of suppression waivers. Both are produced by the facade and consumed here through the contract, so the derivation logic stays a pure transformation of engine results into the audit data with no new engine coupling of its own.
## [2026-06-29T12:36:31.308Z]
Wired the file-aware loop and attestation provenance into the data the page consumes. The pipeline now folds a per-node source-freshness signal into each node's honest state: a node whose mapped bytes changed since its last positive closure is forced to at least the 'we don't know' state, applied even to an unguarded node that owns source, so a whole-repo cached green can never re-render as a pass over a file that was just touched while a known refusal still outranks it. The committed-lock hash and the git commit ref are stamped into the contract's meta so a per-node attestation can be pinned to the exact committed state it attests, with the commit ref honestly absent outside a git tree rather than invented.
## [2026-06-29T15:46:11.555Z]
Corrected fabricated-green and fabricated-zero defects in the portal's data derivation that an adversarial honesty audit surfaced. A container node that owns no source and merely inherits a type-default aspect produced zero verdict-bearing pairs, yet was seeded green 'verified' — so dozens of source-less spine nodes (including the tree root) falsely read as reviewer-approved. A node may now read verified ONLY when it carries at least one real verdict-bearing pair; otherwise it is honestly 'no rule', matching the engine's own vacuous-pair semantics. Separately the suppression / no-rule / not-applicable summary counts were hardcoded to zero, hiding real waivers and unguarded nodes behind a clean facade; they are now derived from the same live data the detail views already list. The portal's core promise is that a green means a reviewer actually checked and approved — these fixes remove every place that asserted it without a real verdict.
## [2026-06-29T20:15:05.819Z]
The per-node attestation panel makes a headline honesty promise — a green verdict cites the exact bytes it attests, and a refused verdict cites the reviewer's reason. The derivation now threads that evidence through every effective-aspect row: a verified row carries the set of subject files the verdict covers (a faithful "these exact bytes"), and a refused row carries the refusal reason. Without this data the panel's drill-through and refusal blocks render nothing, leaving the promise hollow. The covered-bytes set is the union of the per-unit subject files behind the row's collapsed state; the reason is the first non-empty refusal message among the row's units. Aggregate and draft rows carry neither, because they produce no verdict to attest.
