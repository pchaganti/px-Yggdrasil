name: ParserContract
description: "Parser adapters follow a consistent read → parse → validate → return pipeline; any format (YAML, JSON, NDJSON, plain text) must follow the same error and null-safety conventions."
reviewer:
  type: llm
scope:
  per: file
