## [2026-05-31T08:45:05.024Z]
Adds end-to-end coverage of the language-model reviewer mechanics that were previously unprovable without a live model. What is under test is the system's own machinery, not any model's judgment: that an approval verdict is recorded and a refusal blocks with its reason surfaced, that requesting multiple independent opinions issues exactly that many calls and decides by majority, that the prompt actually carries the rule, the component identity, and the source under review, that a transport failure or an unparseable reply degrades to a safe blocking outcome rather than a false pass, that a dormant rule is never sent and an advisory refusal does not block, that a preview never contacts the reviewer, and that a code change re-opens the question. Proving these against the real spawned binary closes the largest remaining blind spot — the reviewer type adopters rely on most heavily was the least exercised end-to-end.
