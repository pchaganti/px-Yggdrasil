name: SchemasCommand
type: command
description: Implements `yg schemas list` and `yg schemas read <name>` —
  read-only access to SCHEMA_TOPICS embedded in the CLI binary.
aspects: []
relations:
  - target: cli/schemas
    type: calls
  - target: cli/formatters
    type: calls
  - target: cli/preamble
    type: calls
  - target: cli/tests/unit/cli
    type: uses
mapping:
  - source/cli/src/cli/schemas.ts
