name: Config & Platform Templates
type: template
description: Default yg-config.yaml template and platform-specific installer
  logic used by yg init. The agent rules content is in the templates (Rules
  Template) sibling node.
relations:
  [
    { target: cli/templates, type: calls },
    { target: cli/utils, type: calls }
  ]
mapping:
  - source/cli/src/templates/default-config.ts
  - source/cli/src/templates/platform.ts
