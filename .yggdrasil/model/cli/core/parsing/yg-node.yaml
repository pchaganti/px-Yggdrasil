name: Core Parsing
type: engine
description: In-memory parser for log.md entries — transforms the pre-read log string into structured entries. No file I/O; the caller reads the file and passes the string. (The when-predicate parsers moved to utils/ so the io layer can import them without an io→engine boundary violation.)
aspects:
  - diagnostic-logging
relations: []
mapping:
  - source/cli/src/core/parsing/log-parser.ts
