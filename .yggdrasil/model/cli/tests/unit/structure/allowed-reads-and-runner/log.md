## [2026-05-30T12:46:26.639Z]
Added regression coverage for the structure reviewer's new suppression behavior. The suite now asserts three things about how source-comment waivers interact with graph-aware deterministic checks: a waiver naming the matching rule removes that rule's finding on the waived line; a waiver naming a different rule leaves the finding intact (waivers are scoped per rule, never blanket); and a finding that names no source line passes through untouched (whole-graph assertions cannot be silenced by a line-anchored marker). These cases lock the wiring itself, independent of the shared suppression module they delegate to, so a future change that mis-routes the rule identity or the line guard fails loudly here rather than silently weakening every author waiver.
