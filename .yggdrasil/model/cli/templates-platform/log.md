## [2026-05-29T22:37:52.435Z]
The init-time templates were separated so the agent-rules content stands alone from the configuration and platform-installer templates, keeping each unit small enough that a reviewer can inspect it in full without context-window truncation. No template content changed; only which node owns which file.
## [2026-05-30T02:29:45.010Z]
The per-node size guard changed from limiting the raw count of mapped source files to limiting the total number of characters in a node's reviewer context — its mapped source files plus the reference files of its effective aspects — and from a non-blocking warning to a blocking error. The motivation is that the reviewer concatenates all of a node's files into each aspect prompt, so an oversized node risks exceeding the model's context window, which deterministically rejects unchanged code near the truncation point; a file-count limit missed this because one very large file could stay under the count while still blowing the prompt. Binary files do not count toward the budget, and a node mapping a single unsplittable generated or binary artifact (a lockfile, an append-only changelog, an image) may carry a documented per-node exemption with a required justification.
## [2026-05-31T16:03:35.346Z]
Replaced the hand-inlined path-separator normalization with calls to a single shared helper. The same small idiom — convert backslash separators to forward slashes, and in most places also strip a trailing slash — had been copied across many modules, so the normalization rule lived in dozens of places at once and any change to it risked drifting them out of step. Consolidating it behind one well-named helper means the rule lives in exactly one spot and each call site reads by intent instead of by a repeated regex. Behavior is unchanged: the helper bodies are byte-for-byte equivalent to the expressions they replace, and the full test suite passes identically.
## [2026-06-19T09:19:58.327Z]
The configuration a fresh project is stamped with now records the new graph schema version and points the reader at the command for the configuration field reference, rather than a per-project schema file path that no longer exists. The version moved because the committed graph format changed when the schemas directory was dropped.
## [2026-06-26T11:34:44.628Z]
No source changes were made to templates-platform in this task. The log gate fires because the deterministic lock cache was cleared and this node's source is counted as changed relative to the last positive closure. The platform template content and the installFor* functions remain unchanged.
## [2026-07-01T11:50:47.724Z]
A newly-initialized project now ships an explicit require-nothing coverage baseline in its generated config, so the very first check on an untouched repository passes with every unmapped file shown as a non-blocking warning instead of failing immediately with a blocking error for every file in the tree. The prior default surfaced a fresh project as a wall of red the moment it was set up — the opposite of the incremental-adoption story the tool intends — even though a require-nothing coverage mode already existed and simply was never written at setup. An absent coverage block still means the whole repository is required, so existing projects are unaffected; only a freshly generated config opts into starting green and tightening coverage as areas are brought under enforcement.
