## [2026-06-01T06:20:25.116Z]
Introduced this module to hold the pure, lossless transform from the previous flat synthetic-key drift baseline into the new typed baseline shape. The old on-disk format stuffed identity elements (own-metadata subset, per-aspect definition and reviewer-tier hashes, deterministic read-set summaries, dependency port-aspect hashes) as prefixed string keys inside the file-hash map; the new format stores them as a typed identity block with a required per-aspect verdict map. Keeping the transform pure and free of I/O lets it live alongside other domain logic rather than inheriting migration-only obligations, while a schema upgrade wires it in. It recomputes the canonical hash over the same logical inputs so unchanged source sees no drift after upgrade.
## [2026-06-03T07:05:21.682Z]
Drift-state rekeying follows the canonical hash change that now folds the recorded verdict and its error source into the per-node hash, so a baseline whose stored verdict was tampered with no longer matches and the gate re-opens instead of trusting a forged pass.
