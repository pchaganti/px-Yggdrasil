name: Config Parsers
type: parser-adapter
description: Parsers for project configuration YAML — yg-config (reviewer tiers,
  providers) and yg-secrets (api keys). Split out from cli/io/parsers so the
  reviewer sees a focused set of files (2) rather than the original 7.
relations:
  - target: cli/model/graph
    type: uses
  - target: cli/utils
    type: calls
mapping:
  - source/cli/src/io/config-parser.ts
  - source/cli/src/io/secrets-parser.ts
