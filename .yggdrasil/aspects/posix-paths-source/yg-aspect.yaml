name: PosixPathsSource
description: "Source files must not use native path separators — forbids path.sep member access and backslash (\\) as a path separator in string literals. Use forward-slash literals or path utilities that emit POSIX output."
reviewer:
  type: deterministic
