## [2026-06-03T08:20:39.987Z]
The aspect-status check tests use a helper that hand-builds a baseline. That helper recorded the canonical hash without folding the per-aspect verdicts it then persisted. For cases that persisted non-empty refused verdicts, the recorded hash no longer matched a recompute over those verdicts, which now surfaces as a blocking integrity error once the gate stops silently dropping unattributable divergences. The helper was corrected to fold exactly the verdicts it persists, matching how a real approve records its final hash, so these honest baselines round-trip and the tests assert the intended aspect-status behavior rather than tripping the new integrity guard.
