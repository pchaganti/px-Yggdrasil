# Scenario document must be well-formed

Each scenario document must begin with a `---` frontmatter block and contain at
least one reproduction step. This is a plain LLM rule with no companion file —
the reviewer sees only the subject document.
