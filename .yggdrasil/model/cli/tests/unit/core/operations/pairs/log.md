## [2026-06-12T13:53:55.876Z]
Updated test suite to match the new PairComputation return shape from computeExpectedPairs (destructure {pairs} in all existing tests). Added three regression tests for the fail-open defect: (a) a content-filter aspect where one candidate file is chmod 0o000 (readable by stat but not readFile) — that file lands in unreadable[] with correct nodePath/aspectId, the readable sibling still produces its pair; (b) the only candidate file is unreadable — zero pairs and non-empty unreadable, preventing a vacuous-green result; (c) a pure path-filter aspect never produces unreadable records even when a file is chmod 0o000, because path evaluation never calls readFile. Chmod is restored in a finally block before afterEach cleanup.
