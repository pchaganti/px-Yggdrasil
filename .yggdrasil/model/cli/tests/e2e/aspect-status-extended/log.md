## [2026-05-31T08:36:52.870Z]
Adds end-to-end coverage for how a rule's enforcement level combines and changes. The same rule can reach a component from several directions, and when those directions disagree on whether it is dormant, advisory, or blocking, the strictest must win; flipping a rule's level must re-engage every affected component, and a recorded failure must persist so a later check renders it without re-asking the reviewer. This suite spawns the real binary to prove the level-combination and transition mechanics, because a mistake here either hides a real failure or blocks on a rule that was meant to be silent.
## [2026-05-31T09:10:49.744Z]
Updated the suite to assert the corrected behavior after fixing product defects surfaced by this comprehensive E2E effort. The suite previously pinned the actual (defective) behavior so it would stay green either way; now that the defects are fixed, the assertions assert the intended contract instead, so the suite guards the corrected behavior going forward.
## [2026-05-31T09:59:02.189Z]
Updated the suite to track and assert the new drift-fingerprint shape: a rule's definition metadata is fingerprinted with its enforcement level excluded (so a level flip is not drift), recorded as a definition synthetic rather than the raw rule file. The assertions were adjusted to the corrected behavior accordingly.
