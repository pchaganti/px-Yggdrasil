name: Relations Unit Tests — Resolver
type: test-suite
description: "Unit tests for the relation resolver: the tri-state classify/resolve
  contract (resolved / ambiguous / absent), symbol- and path-axis resolution, the D7
  unmapped-target non-event, and the Ruby root-anchoring guard. Split from the relations
  unit-test node so each stays within the reviewer prompt limit."
aspects: []
relations:
  - target: cli/relations/core
    type: uses
mapping:
  - source/cli/tests/unit/relations/resolver.test.ts
