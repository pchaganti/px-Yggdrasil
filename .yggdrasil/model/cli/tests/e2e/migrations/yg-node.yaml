name: E2E Tests — Migrations
type: test-suite
description: "Subprocess E2E proving init --upgrade migration paths — v4.3 to v5 config migration, idempotency, withheld-bump on a bad config (now surfaced with exit 1), aspect reviewer ast to deterministic and absent to llm, version-too-new rejection by check and tree, and drift-state baseline re-keying (flat synthetic-key shape to typed with pre-verdict approved-synthesis, verdict preservation, unparseable-baseline deletion that withholds the bump then recovers on re-run, and idempotent skip of an already-typed baseline)."
aspects: []
relations: []
mapping:
  - source/cli/tests/e2e/cli-migrations.test.ts
