name: KnowledgeCommand
type: command
description: Implements `yg knowledge list` and `yg knowledge read <name>` — read-only access to KNOWLEDGE_TOPICS embedded in the CLI binary.
aspects: []
relations:
  - target: cli/templates
    type: calls
  - target: cli/tests/unit/cli
    type: uses
mapping:
  - source/cli/src/cli/knowledge.ts
