name: SingleSourceGraphQueries
description: "Engine files must not redefine reserved graph-query helpers — these live only under core/graph/. Prevents future divergent implementations of node-hierarchy walks, flow lookups, aspect resolution, and file enumeration."
reviewer: ast
language: [typescript]
when:
  node:
    type: engine
